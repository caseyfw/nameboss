var db = require('./names.json');
var names = {

    // Return all future names.
  //   var all = (callback) => {
  //       callback(db.names.slice(db.nextId));
  //         }
  //
  //         }
  //
